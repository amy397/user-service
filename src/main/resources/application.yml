server:
  port: 8081

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://127.0.0.1:3307/user_db?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: mySecretKeyForJwtTokenGenerationMustBeLongEnough256BitsKey
  expiration: 86400000